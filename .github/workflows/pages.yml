# NOTE: Replaced by a gh-pages deployment workflow that avoids usage of deprecated
# actions/upload-artifact v3 internals. See `deploy-gh-pages.yml` for the new flow.
# The original pages workflow is intentionally left as a placeholder to document
# the change.

name: Old Pages Workflow (disabled)

on: []

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use deploy-gh-pages.yml instead."